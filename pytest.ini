[pytest]
markers =
    unit: pruebas unitarias, sin dependencias externas (red, base de datos, LLM real)
    integration: pruebas de integracion, requieren PGVector de pruebas en Docker (puerto 5433)
testpaths = tests
pythonpath = src
